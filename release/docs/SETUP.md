# Setup

The application expects the project root to contain `backend`, `frontend`, `uploads`, `docs`, and `tests`.

For a clean local install, create `.venv`, install `backend/requirements.txt`, then run the Windows launcher or `python backend/app.py`.
